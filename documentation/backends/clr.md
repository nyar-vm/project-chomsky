# CLR 后端

针对 .NET Runtime 的后端实现。

## 特性

- **CIL 生成**：输出可验证的 IL 与元数据。
- **类型与异常语义**：把类型系统与异常传播纳入约束与具体化决策。
- **互操作边界**：把 P/Invoke、托管/非托管边界的资源与 ABI 约束显式建模。

## 当前实现状态（诚实基线）

当前实现以演示级生成为主；完整的验证通过、泛型/反射语义与性能导向成本模型仍在演进中。
