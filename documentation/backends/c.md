# C 后端

作为最通用的降级（Fallback）方案，C 后端的定位是：在缺乏更强目标后端时，仍能把已具体化的 iKun 稳定落地为可移植实现。

## 特性

- **标准兼容**：生成符合 ISO C99 标准的代码。
- **无依赖**：生成的代码不依赖于复杂的运行库。
- **资源语义显式化**：把生命周期、借用与释放策略转化为可检查的代码结构。

## 适用场景

适用于嵌入式系统或需要高度可移植性的原始库开发。
