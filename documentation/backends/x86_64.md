# x86_64 后端

针对现代桌面和服务器 CPU 的原生后端。该后端的职责是把“已具体化”的 iKun 降低为指令选择、寄存器分配与目标文件产物。

## 技术亮点

- **目标产物生成**：输出可链接的目标文件或机器码片段。
- **成本模型可插拔**：以微架构参数驱动提取阶段选择最优实现。
- **具体化承接**：布局/调度决策在进入后端前应已确定。

## 当前实现状态（诚实基线）

当前实现以演示级产物生成为主，仍缺少完整的指令选择、寄存器分配与调度体系；文档以目标能力为准，具体落地以 readmap 演进计划为准。
