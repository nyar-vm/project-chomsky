# JVM 后端

将优化后的 UIR 转换为 Java 字节码。

## 特性

- **字节码生成**：输出可验证的 JVM 字节码与必要的运行时支撑。
- **运行时兼容**：面向 OpenJDK 与 GraalVM 的成本模型与具体化策略可分离。
- **并发语义对齐**：把并行意图在约束允许时映射到线程/向量/协程等实现选择。

## 当前实现状态（诚实基线）

当前实现以演示级生成为主；完整的字节码验证、异常语义、对象模型与性能导向的成本模型仍在演进中。
